<%= form_with(model: boot_test, local: true) do |form| %>
  <% if boot_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boot_test.errors.count, "error") %> prohibited this boot_test from being saved:</h2>

      <ul>
      <% boot_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
